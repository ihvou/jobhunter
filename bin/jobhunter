#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"

cat >&2 <<'EOF'
`./bin/jobhunter` is deprecated after the OpenClaw Phase 2 cleanup.
Use `./bin/openclaw` instead.
EOF

exec "$SCRIPT_DIR/openclaw" "$@"
