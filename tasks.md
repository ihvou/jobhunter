# Tasks

> **Note**: open work only. Completed (`Done`) and `Superseded` rows have been archived — find them in `git log` (search by row number, e.g. `git log -S "| 76 | Done"`). Do not add narrative sections, sub-headings, or freeform notes outside the table. New work goes in as a new numbered row; numbering continues from the highest used number across history.

| # | Priority | Problem | Solution | Acceptance | Impact |
|---:|---|---|---|---|---|
| 44 | P2 | `JobHunter` ([app.py](jobhunter/app.py)) is becoming a god object. With the new bot-level message actions, `disc:*`/`tune:*` callbacks, and discovery/tuning request writers, it gets worse. | Split into `Collector`, `FeedbackHandler`, `DiscoveryCoordinator`, `ScoringCoordinator`, `Reporter`. `JobHunter` becomes a thin wiring layer. | Each new module has its own unit tests; `app.py` < 80 lines. | New flows pile on, get untestable. |
| 47 | P2 | No linter/formatter. `pyproject.toml` only configures pytest. | Add `[tool.ruff]` with a small rule set; CI step `ruff check`. | `ruff check` passes on the tree. | Style drift; unused imports linger. |
| 49 | P3 | Spec Phase 5: CV ingestion in any format (`cv.pdf`, `cv.docx`, `cv.doc`, public URL). | Detect format on `init`; extract to `cv.text-cache.md` using `pdftotext` / `python-docx` / fetch+strip. Cache extracted text. | Test: a stub PDF with known text yields a matching `cv.text-cache.md`. | Removes manual "export your CV to text" step. |
| 50 | P3 | Spec Phase 5: multi-profile variants. | Support `input/profile.<variant>.md`; route by job cluster (e.g. detected via the matching scoring rule IDs). | Test: cover note for an "AI" cluster job uses `profile.ai.md`. | Different role clusters need different framings. |
| 51 | P3 | Spec Phase 4: CV bullet suggestions. | New action `bullet_suggestions` mirroring `cover_note`; same budget gate; produces 3-5 alternative CV bullets per role. | Test: stubbed LLM returns 3 bullets persisted to `drafts`. | Phase 4 deliverable. |
| 52 | P3 | Spec Phase 4: company research summary; application checklist. | New actions `company_research` (fetch + summarise) and `checklist` (5-item per-job pre-application list). | Tests for each. | Phase 4 deliverables. |
| 62 | P1 | [database.py](jobhunter/database.py) `rate_limit_daily` queries `job_feedback` for `action='cover_note'`. cover_note can be requested unlimited times for the same job and each adds a row, inflating the daily counter against unrelated jobs. | Either (a) decrement-style counter via a dedicated `daily_counters` table keyed by `(action, day)`, or (b) pass `distinct job_id` count rather than raw row count when rate-limiting cover notes. (b) is simpler. | Test: requesting cover-note for the same job 3 times in one day counts as 1 toward the 10/day cap (or fails idempotently). | A user iterating on one cover note via re-requests can prematurely hit the daily cap. |
| 66 | P1 | [config.py](jobhunter/config.py) `parse_profile_description` is hardcoded to product/AI keywords from the example profile. A user with a "data engineer" or "designer" profile gets no useful auto-extraction. The merge with hand-authored `profile.local.json` saves the day — but the function's name suggests it works for any description. | Either (a) document the limitation in the function docstring and rename to `parse_product_focused_description`, or (b) generalize: add a generic title-pattern regex `\b([a-z][a-z0-9 +#.-]{1,40}\s+(engineer|developer|designer|manager|lead|owner|architect|scientist|analyst|specialist))\b` and pull keywords from the description by tokenizing and intersecting with a small built-in set of tech terms. | Test: `parse_profile_description("Senior data engineer with Python and Snowflake.")` returns `target_titles=["senior data engineer"]` and `positive_keywords` containing `python`, `snowflake`. | Onboarding works only for the example product profile. |
| 72 | P2 | [database.py](jobhunter/database.py) canonical URL strips `utm_*`, `ref`, `source`, `fbclid`, `gclid`. Misses other common tracking params: `mc_cid`, `mc_eid`, `_hsenc`, `_hsmi`, `igshid`, `vero_id`, `_ke`, `mkt_tok`, `__hssc`, `__hstc`. | Replace the per-key allowlist with a denylist of tracking-param prefixes/exact names. Easy: `if key.lower().startswith(("utm_","_hs","__hs","mkt_")) or key.lower() in {"ref","source","fbclid","gclid","mc_cid","mc_eid","igshid","vero_id","_ke"}: skip`. | Test: URL `https://x.com/job?_hsenc=abc&utm_id=1&id=42` canonicalizes to `https://x.com/job?id=42`. | Slightly more dedupe accuracy across newsletter/email-shared URLs. |
| 73 | P2 | [sources.py](jobhunter/sources.py) `wait_for_host_rate_limit` uses module-level `HOST_LAST_FETCH` dict. Multi-instance deploys would share nothing (each container has its own). Acceptable today (single instance). Worth flagging in a comment for future readers. | Either: (a) leave it, add a one-line comment "process-local; not safe across multiple containers", or (b) move to the `rate_limits` DB table for cross-container coordination. (a) is fine for MVP. | Comment present in code; behavior unchanged. | Hidden assumption that future readers might miss when scaling out. |
| 75 | P2 | No live smoke test target. Real source schemas drift over time (Remotive renames a field, RemoteOK changes its API shape) and we'd find out only when users notice. Particularly relevant after #127-style per-source parsing changes (cf. the DOU rss_proxy ship 2026-05-26 that surfaced no broken-title rows but could have on a different RSS shape). | Add `tests/test_smoke.py` requiring `JOBHUNTER_RUN_LIVE=1` to opt in. For each enabled source in the default `config/sources.json`, hit the real endpoint, assert ≥1 job returned, assert at least 80% have non-empty title and URL. Skip with `unittest.skipUnless(os.getenv("JOBHUNTER_RUN_LIVE"))`. | `JOBHUNTER_RUN_LIVE=1 python3 -m pytest tests/test_smoke.py -v` runs real fetches and passes. Without the env var, the tests skip cleanly. | Source-format regressions caught in CI/dev rather than in production digests. |
| 78 | P2 | [app.py](jobhunter/app.py) calls `recalculate_source_scores` after every Applied / Irrelevant / cover-note completion. Each call aggregates `source_feedback_metrics` across **all** jobs and feedback rows ever seen, then issues one `update sources set score = ?` per source. Cheap today (hundreds of jobs); slow as the DB grows (10K+ jobs × dozens of sources → full join + group-by per click). | Add lightweight debouncing on the `JobHunter` instance: a `last_recalc_at` timestamp, skip if last call was < 30 seconds ago. Or move recalc to a background thread that drains a small queue. Keep current behavior as fallback for low-activity periods. | Test asserts that 10 rapid Applied clicks within 30s trigger only one DB update on `sources.score`; after 31s, the next click triggers another. | Click latency degrades with DB size; subjectively the bot feels slow on each Applied click after a month of use. |
| 83 | P3 | [app.py](jobhunter/app.py) defaults `min_show_score` to 0 if `config/scoring.json` is missing or has no `thresholds.min_show_score`. After earlier fix this means a corrupt/missing scoring.json silently turns into "show every job" — every score ≥ 0. The DEFAULT_RULES in [scoring.py](jobhunter/scoring.py) has `min_show_score: 50`, so loading the empty default is safe; but a user-written scoring.json that omits the threshold would silently drop the floor. | Either: (a) raise the empty-thresholds default in [app.py](jobhunter/app.py) to 30 (sensible floor), or (b) fail loud at startup if `scoring.json` exists but is missing required keys (`rules`, `thresholds`) — log warning to stdout and Telegram-send a one-time "Scoring config malformed; using baseline." | With a `scoring.json` missing `thresholds`, the digest still applies a 30-point floor; with a `scoring.json` missing `rules`, the bot logs/sends a clear startup warning. | A misconfiguration silently loosens the filter; user sees lots of low-quality jobs and doesn't know why. |
| 112 | Partial | **Dual-run scoring window after Apply + drift alert + scoring history/revert + widen shadow-test.** Bundle of safety nets around scoring updates. | (a) For 7 days post-Apply (configurable `JOBHUNTER_SCORING_DRIFT_WINDOW_DAYS`), score every new job under both v_old and v_new. Persist diffs to `scoring_drift(job_id, v_old_id, v_old_score, v_old_hard_reject, v_new_id, v_new_score, v_new_hard_reject, scored_at)`. (b) Daily check: if ≥3 jobs in last 24h are hard-rejected by v_new but would have passed v_old with score ≥ threshold, send Telegram alert "Scoring v_N has hard-rejected M jobs that v_(N-1) would have shown. [Show diff] [Revert] [Keep]." (c) `/scoring history`: list versions with timestamps + headline shadow-test number. (d) `/scoring revert`: rolls to most recent archive; bumps version forward (v3 → v5) and inserts an audit row in `agent_actions`. (e) Widen shadow-test sample from 100 → 500 in `ScoringCoordinator.shadow_test`. | Tests: drift table accumulates rows; alert fires on ≥3 drifted jobs; revert restores the previous file and audit row appears in `/history`. | Catches "Codex's new rules silently filter out good jobs" — the user's explicit concern. |
| 114 | Partial | **`backup_export` plugin tool.** A user-triggerable backup escape hatch. | New `jobhunter_backup_export` plugin tool with payload `{include: ['config'\|'input'\|'scoring_archives'\|'database']}`. Creates timestamped tar.gz at `data/backup/jobhunter-<ts>.tar.gz`. Returns path + size. Does NOT include `data/jobs.sqlite` by default unless explicitly requested (avoid huge files). | Tests: triggering via plugin tool produces a tar.gz with the right contents. | Easy backup escape hatch for users who want to snapshot before risky edits. |
| 127 | P1 | **Source-specific parsing quirks keep being shipped as Python patches.** Recurring pattern: each new source quirk = code change + release + restart. Most recent example: 2026-05-26 DOU rss_proxy ship — a whole new source type for one site's Cloudflare gating. Per-feed company selectors, SPA detection params, etc. — each is a release. The agent could observe a fetched page and propose a fix, but today it has no data shape to write into. | Move per-source extraction logic into bounded data, interpreted by fixed Python. (a) Add optional `extraction_config` field to source rows with a small DSL: `{title_strategy: {kind: 'split', separator: ': ', side: 'right'\|'left', stopwords: [...]}, company_strategy: {...}, description_extractor: {kind: 'regex'\|'css'\|'literal', pattern: ...}, spa_detection: {min_links: int, max_body_chars: int, framework_markers: [...]}, fetch_via: 'urllib'\|'firecrawl_markdown'\|'firecrawl_raw_html'}`. (b) Fixed Python interpreter in [sources.py](jobhunter/sources.py); default config preserves current behavior so existing tests pass. (c) New `source_extraction_proposal` action kind: agent observes a fetched sample (via `firecrawl_scrape_markdown`) and proposes a config; user approves; persisted to the source row. (d) Schema validation rejects unknown DSL keys at apply-time. | Source row with `extraction_config: {company_strategy: {kind: 'split', separator: ':', side: 'left'}}` extracts company=Toptal from "Toptal: QA Automation Engineer" without touching `infer_company`. Agent flow: `Update sources` → produces `source_extraction_proposal` action → approval persists the new config. DOU could be expressed as `fetch_via: 'firecrawl_raw_html'` + `extraction_config` rather than a hardcoded `rss_proxy` type. Existing tests pass. | Stops shipping releases for every new source quirk. Lets the agent self-improve scraping quality. Closes a recurring class of code-vs-config friction. |
| 142 | Partial | **Index-then-serve architecture.** Today: collect runs L1, then digest time runs L2 lazily on top-30 candidates → Get more jobs takes 30-90s and produces inconsistent results because L2 is half-applied. Need to flip: index eagerly (collect + L1 + L2 all at index time), serve from cache. | Implemented safe slice: collection indexes capped L2 for new jobs above L1 cutoff, digest is pure SELECT, Get more jobs instantly serves indexed jobs and queues background refresh when stale, `/refresh` forces refresh, and `JOBHUNTER_COLLECT_STALE_MINUTES` controls the stale gate. Remaining gap: scoring/profile rescore currently runs synchronously for preview rather than fully background. | Tests cover indexed digest, stale refresh routing, and updated command parsing; a deeper async-rescore test remains. | Big UX improvement landed without a risky orchestration rewrite. |
| 143 | Partial | **Behavior + smoke + perf tests.** Current tests are wiring tests. Each user-visible regression that shipped (scoring drift, silent rate-limit refusal, sources blocked by robots.txt, agent-added sources also blocked) wasn't caught by wiring tests. | Added `tests/test_behavior.py` for top-10 fallback, L2 demotion visibility, snoozed resurfacing, and 1000-job digest query performance. Added `tests/test_smoke.py` gated by `JOBHUNTER_RUN_LIVE=1` for live source parseability. Remaining gap: no CI workflow wiring and no live deterministic agent smoke. | Local unittest suite passes with smoke skipped unless opted in. | Better regression net for the exact failures seen, with CI/live-agent hardening still open. |
| 145 | Partial | **No preview after scoring/profile change.** When scoring_rule_proposal applies, jobhunter rescores existing jobs ("Rescored 353 jobs; up=19, down=204") but the user has to manually tap Get more jobs to see the impact. | Implemented top-5 preview after scoring/profile applies and confirmed applies; includes both `/revert <id>` guidance and an inline Revert button when an agent action id is available. Remaining gap: legacy tuning apply has no agent action id, and profile L2 does not forcibly invalidate cached L2 verdicts yet. | Preview is sent immediately after apply; agent-action previews include a tap-to-revert button. | Makes scoring/profile changes visible immediately, with legacy tuning revert still command/audit limited. |
| 146 | Partial | **Unified scoring pipeline (the big refactor).** Today's pipeline mixes hard filters (`min_show_score`, `hard_reject`, L2 verdict allowlist, digest_log filter), categorical overlays (L2 verdict tiers, L2 priority tiers), and compound sort. Failure modes are silent: scoring v3 misset → empty digest → no idea why. The user proposed a cleaner model: one score 0-100 (L1: 0-50 + L2: 0-50), no hard filters, sort by score, always show top 10. | Implemented v7/v8 columns on `jobs` (`l1_score`, `l2_score`, generated `total_score`, `l2_reason`, `l1_rules_fired_json`), backfill, queryable unified rank, digest order by generated total score, no `min_show_score`/hard_reject/L2 verdict filter in digest, and score card shows L1+L2. `min_show_score` now logs a deprecation warning when loaded. Remaining gaps: L2 still returns verdict/priority internally and maps to numeric score, `hard_reject` remains transitional audit data, and `config/scoring.json` may still contain thresholds for compatibility. | Tests cover generated total-score integrity, top-10 fallback, total-score ordering, L2 demotion, snoozed resurfacing, deprecation warning, and digest query performance. | Removes the silent-empty digest mode while keeping the refactor low-risk and backward-compatible. |
| 154 | Partial | **Engineer agent runtime is misconfigured — all cron runs exit after planning, never ship a PR.** Verified live in 4 manual triggers on 2026-05-26: engineer cron picks a task, prints a planning message ("Picked task `6`: ... I'm going to branch from `origin/main`, then inspect..."), and exits with `status=ok` after 30-170s. No branch, no commit, no PR ever materializes. agent_tasks status moves `open → picked → never to completed`, leaving tasks stuck. Root cause: `openclaw.json` `agents.defaults.cliBackends.codex-cli` had `CODEX_HOME=/home/node/.openclaw/agents/main/agent/codex-home` (not engineer's) and CLI args `--sandbox read-only --skip-git-repo-check`. Engineer's own `agents/engineer/agent/codex-home/config.toml` (`sandbox = "workspace-write"` + `approval_policy = "never"`) was dead code because CLI flags override config.toml. | Implemented config/docs/tests for the OpenClaw 2026.5.7 path: regular agents stay on `openai-codex/gpt-5.5` app-server; Engineer is the only agent selecting `codex-cli/gpt-5.5`; shared `agents.defaults.cliBackends.codex-cli` is wired to Engineer `CODEX_HOME`, `HOME`, `--sandbox workspace-write`, and `--cd /workspace`. The OpenAI plugin owns `codex-cli` `bundleMcp`/native-tool registration, so this preserves Jobhunter plugin tools for Engineer while changing only command/env/args. `ensure-engineer-workspace` also writes both `sandbox` and `sandbox_mode` plus `approval_policy=never` into Engineer Codex config. Remaining: apply onboard config to the live gateway and run the live cron/PR acceptance. | Manually retrigger engineer cron with an existing open `to_agent='engineer'` task in agent_tasks; within the 60min cron timeout, expect (a) a real `engineer/<task-id>-<slug>` branch pushed to `origin`, (b) an open PR, (c) `agent_tasks.id` status moves to `completed` with `result_json` containing `pr_url`. Other agents' subsequent cron runs continue to succeed read-only. All existing tests still pass. | Tracker remains Partial until trajectory/log evidence shows the Engineer cron used `codex-cli/gpt-5.5` in `/workspace`, pushed a branch, opened a PR, and completed the task. |
| 155 | P2 | **QA agent is data-quality-only; misses operational anti-patterns that are DB-observable.** Today's QA cron prompt: "run jobhunter_audit_email_extraction, inspect suspicious recent rows with SELECT-only SQL, file qa.bug tasks". Caught task #4 (email backlog) but missed many DB-observable bugs we caught manually: engineer cron stuck in `picked` (tasks.md #154), DOU source returning 403 on `type=rss` switch, broken `'Відгукнутись на вакансію'` titles, lead rescore wave hangs. Pattern: QA's prompt is too vague ("inspect suspicious rows") so Codex doesn't enumerate enough checks. | Rewrite the QA cron prompt to explicitly enumerate DB anti-patterns to check each night, with concrete SELECT-only SQL templates: (a) tasks stuck in `status='picked'` with `picked_at < -24h and completed_at is null`, (b) `source_runs` with `error is not null` for ≥2 consecutive runs (last 24h), (c) `jobs` with placeholder titles (regex match common boilerplate: `Apply for|Відгукнутись|Apply now|Submit|Application`), (d) `email_alert_raw` with `parsed_at is null and received_at < -48h`, (e) `agent_reports` with summary containing `error|failed|skipped|stuck`, (f) sudden drops in `jobs_for_digest` count (compare today vs trailing 7-day avg). Each anti-pattern produces a `qa.bug` task with `kind`-specific payload (sample row ids, anti-pattern name, observed-vs-expected). No new tools, no schema changes. | After one nightly QA run with a deliberately-broken state (insert a `status='picked'` row aged >24h), QA files a `qa.bug` task naming that anti-pattern with the row id. No false positives on healthy state. Existing data-quality checks (email extraction audit) still run alongside the new ones. | Most bugs we currently catch manually are DB-observable. Expanding QA's prompt closes that gap without new infra. Direct lift to autonomous-team value: PM stakeholder reports become more substantive. |
| 156 | P3 | **QA can't read logs or chat content — design pass needed for sensitive-data handling.** QA can SELECT the DB but has no tools to read gateway logs (`docker logs openclaw-gateway`), Codex session transcripts, raw Telegram message history, or container resource metrics. Whole classes of failures stay invisible to QA: Codex 30s tool timeouts (visible in logs only), CAPTCHA / 403 responses (logs), session wedges (last_phase in logs), user complaints in Telegram that don't manifest as DB anomalies, prompt-injection attempts in lead pitch text. Even if Codex correctly identifies them, the bot can't read them. | Design memo answering: (a) which signals are worth exposing (logs vs chat vs metrics), (b) sensitive-data handling (token redaction regex on log reads, prompt-injection mitigation when reading user-controlled text like lead pitches), (c) tool surface (`qa_read_gateway_logs(since, grep, limit)` with built-in redaction; `qa_recent_chat_messages(limit)` with size cap and PII flag), (d) failure modes (rate limiting on log reads to avoid container CPU spike, defensive parsing of Telegram message HTML), (e) which agents get access (QA only? PM too?). Implementation in a follow-up after design lands. Should not enable any new exec/shell capabilities. | A written design memo (~2 pages) covering the above. Includes a redaction allowlist of token-shape patterns (AWS access key, GitHub PAT, Codex bearer, OpenAI key, Firecrawl key, Telegram bot token, IMAP creds). Includes a worked example of each tool's input/output. Explicit non-goal: writing the implementation. | Catches operational issues invisible to DB-only QA: 403 streaks, timeouts, session wedges, user friction. But cost is real (sensitive data exposure, prompt-injection surface). Worth the design pass before implementation. |
