# Tasks

> **Note**: open work only. Completed (`Done`) and `Superseded` rows have been archived — find them in `git log` (search by row number, e.g. `git log -S "| 76 | Done"`). Do not add narrative sections, sub-headings, or freeform notes outside the table. New work goes in as a new numbered row; numbering continues from the highest used number across history.

| # | Priority | Problem | Solution | Acceptance | Impact |
|---:|---|---|---|---|---|
| 44 | P2 | `JobHunter` ([app.py](jobhunter/app.py)) is becoming a god object. With the new bot-level message actions, `disc:*`/`tune:*` callbacks, and discovery/tuning request writers, it gets worse. | Split into `Collector`, `FeedbackHandler`, `DiscoveryCoordinator`, `ScoringCoordinator`, `Reporter`. `JobHunter` becomes a thin wiring layer. | Each new module has its own unit tests; `app.py` < 80 lines. | New flows pile on, get untestable. |
| 47 | P2 | No linter/formatter. `pyproject.toml` only configures pytest. | Add `[tool.ruff]` with a small rule set; CI step `ruff check`. | `ruff check` passes on the tree. | Style drift; unused imports linger. |
| 49 | P3 | Spec §14 Phase 5: CV ingestion in any format (`cv.pdf`, `cv.docx`, `cv.doc`, public URL). | Detect format on `init`; extract to `cv.text-cache.md` using `pdftotext` / `python-docx` / fetch+strip. Cache extracted text. | Test: a stub PDF with known text yields a matching `cv.text-cache.md`. | Removes manual "export your CV to text" step. |
| 50 | P3 | Spec §14 Phase 5: multi-profile variants. | Support `input/profile.<variant>.md`; route by job cluster (e.g. detected via the matching scoring rule IDs). | Test: cover note for an "AI" cluster job uses `profile.ai.md`. | Different role clusters need different framings. |
| 51 | P3 | Spec §14 Phase 4: CV bullet suggestions. | New action `bullet_suggestions` mirroring `cover_note`; same budget gate; produces 3-5 alternative CV bullets per role. | Test: stubbed LLM returns 3 bullets persisted to `drafts`. | Phase 4 deliverable. |
| 52 | P3 | Spec §14 Phase 4: company research summary; application checklist. | New actions `company_research` (fetch + summarise) and `checklist` (5-item per-job pre-application list). | Tests for each. | Phase 4 deliverables. |
| 53 | P3 | Spec §14 Phase 5: opt-in `JOBHUNTER_DAILY_REFRESH=1` background collection as a safety net for users who go a few days without clicking. | Add a separate thread that runs `collect()` once per day if the flag is set; rate limit shared with `Get more jobs`. | Test: with the flag set, a 26-hour test loop triggers exactly one background collect. | Optional convenience. |
| 62 | P1 | [database.py:417-424](jobhunter/database.py:417) `rate_limit_daily` queries `job_feedback` for `action='cover_note'`. But `cover_note` is recorded **twice** per cycle: once when the user clicks the button (`add_feedback(job_id, "cover_note")` at [app.py:264](jobhunter/app.py:264) inside `generate_cover_note`) — wait, let me re-check. Actually clicking the button doesn't record feedback, only successful generation does. But `rate_limit_daily` fires at click time before generation. Re-check still shows: `app.py:233` calls `rate_limit_daily("cover_note", ...)` which counts existing feedback rows; each successful generation adds one. So clicks are gated by past *successful* counts. Mostly fine. **Real issue**: `add_feedback` rejects duplicate `applied`/`irrelevant` for the same job but cover_note can be requested unlimited times for the same job and each adds a row, inflating the daily counter against unrelated jobs. | Either (a) decrement-style counter via a dedicated `daily_counters` table keyed by `(action, day)`, or (b) pass `distinct job_id` count rather than raw row count when rate-limiting cover notes. (b) is simpler. | Test: requesting cover-note for the same job 3 times in one day counts as 1 toward the 10/day cap (or fails idempotently — see #63). | A user iterating on one cover note via re-requests can prematurely hit the daily cap. |
| 66 | P1 | [config.py:243-271](jobhunter/config.py:243) `parse_profile_description` is hardcoded to product/AI keywords from the example profile. A user with a "data engineer" or "designer" profile gets no useful auto-extraction. The merge with hand-authored `profile.local.json` saves the day — but the function's name suggests it works for any description. | Either (a) document the limitation in the function docstring and rename to `parse_product_focused_description`, or (b) generalize: add a generic title-pattern regex `\b([a-z][a-z0-9 +#.-]{1,40}\s+(engineer|developer|designer|manager|lead|owner|architect|scientist|analyst|specialist))\b` and pull keywords from the description by tokenizing and intersecting with a small built-in set of tech terms. | Test: `parse_profile_description("Senior data engineer with Python and Snowflake.")` returns `target_titles=["senior data engineer"]` and `positive_keywords` containing `python`, `snowflake`. | Onboarding works only for the example product profile. |
| 68 | P1 | [coordinators.py:135-161](jobhunter/coordinators.py:135) `ScoringCoordinator.poll_done` uses a `notified-<sid>` marker file as the "already-shown" sentinel; [coordinators.py:56-95](jobhunter/coordinators.py:56) `DiscoveryCoordinator.poll_done` uses the `discovery_runs` DB row. Two patterns for the same problem. | Pick one. Recommend: add a `tuning_runs` table mirroring `discovery_runs` and switch `ScoringCoordinator` to the DB-row pattern; remove the marker-file mechanism. | Test: same as discovery — second `poll_done` after a session is marked `done` and notified does not re-emit. | Two parallel mechanisms make the OpenClaw worker contract harder to document and harder to test. |
| 69 | P1 | [database.py:401-415](jobhunter/database.py:401) `rate_limit_check` increments `count_24h` on every call but **nothing ever resets or queries it**. Column is dead. | Either drop the column (single-line schema change at the next migration) or make it the basis of `rate_limit_daily` (cleaner: now both rate limiters use the same table; daily cap is `count_24h` reset at midnight UTC). | Test: at midnight UTC, daily counters reset; calling `rate_limit_daily` then returns `(True, 0)`. | Either dead code or hidden state that grows with no purpose. |
| 72 | P2 | [database.py:670-682](jobhunter/database.py:670) canonical URL strips `utm_*`, `ref`, `source`, `fbclid`, `gclid`. Misses other common tracking params: `mc_cid`, `mc_eid`, `_hsenc`, `_hsmi`, `igshid`, `vero_id`, `_ke`, `mkt_tok`, `__hssc`, `__hstc`. | Replace the per-key allowlist with a denylist of tracking-param prefixes/exact names. Easy: `if key.lower().startswith(("utm_","_hs","__hs","mkt_")) or key.lower() in {"ref","source","fbclid","gclid","mc_cid","mc_eid","igshid","vero_id","_ke"}: skip`. | Test: URL `https://x.com/job?_hsenc=abc&utm_id=1&id=42` canonicalizes to `https://x.com/job?id=42`. | Slightly more dedupe accuracy across newsletter/email-shared URLs. |
| 73 | P2 | [sources.py:606-615, 27](jobhunter/sources.py:606) `wait_for_host_rate_limit` uses module-level `HOST_LAST_FETCH` dict. Multi-instance deploys would share nothing (each container has its own). Acceptable today (single instance). Worth flagging in a comment for future readers. | Either: (a) leave it, add a one-line comment "process-local; not safe across multiple containers", or (b) move to the `rate_limits` DB table for cross-container coordination. (a) is fine for MVP. | Comment present in code; behavior unchanged. | Hidden assumption that future readers might miss when scaling out. |
| 75 | P2 | No live smoke test target. Real source schemas drift over time (Remotive renames a field, RemoteOK changes its API shape) and we'd find out only when users notice. | Add `tests/test_smoke.py` requiring `JOBHUNTER_RUN_LIVE=1` to opt in. For each enabled source in the default `config/sources.json`, hit the real endpoint, assert ≥1 job returned, assert at least 80% have non-empty title and URL. Skip with `unittest.skipUnless(os.getenv("JOBHUNTER_RUN_LIVE"))`. | `JOBHUNTER_RUN_LIVE=1 python3 -m pytest tests/test_smoke.py -v` runs four real fetches and passes. Without the env var, the tests skip cleanly. | Source-format regressions caught in CI/dev rather than in production digests. |
| 78 | P2 | [app.py:222, 261, 322](jobhunter/app.py:222) call `recalculate_source_scores` after every Applied / Irrelevant / cover-note completion. Each call aggregates `source_feedback_metrics` across **all** jobs and feedback rows ever seen, then issues one `update sources set score = ?` per source. Cheap today (hundreds of jobs); slow as the DB grows (10K+ jobs × dozens of sources → full join + group-by per click). | Add lightweight debouncing on the `JobHunter` instance: a `last_recalc_at` timestamp, skip if last call was < 30 seconds ago. Or move recalc to a background thread that drains a small queue. Keep current behavior as fallback for low-activity periods. | Test asserts that 10 rapid Applied clicks within 30s trigger only one DB update on `sources.score`; after 31s, the next click triggers another. | Click latency degrades with DB size; subjectively the bot feels slow on each Applied click after a month of use. |
| 82 | P3 | Privacy gap: [coordinators.py:34, 117](jobhunter/coordinators.py:34) include `self.profile.raw_text[:6000]` in both discovery and tuning request payloads. Per spec §13.2 the cover-note path was hardened (#15) to not forward the full free-text profile, but the discovery/tuning handoffs still do — the entire profile description gets pasted to ChatGPT during a manual handoff. By design today (Codex needs the description to find good sources), but worth noting that what reaches each LLM differs by flow. | Two-part: (a) document in [AGENTS.md](AGENTS.md) and [README.md](README.md) Privacy section: "discovery/tuning handoff sends the full `input/profile.local.md` to whatever LLM you paste it into; cover notes send only the structured profile + bounded CV excerpt." (b) Optional: add `JOBHUNTER_DISCOVERY_INCLUDE_DESCRIPTION=0` env to send only the structured profile (`target_titles + positive_keywords + exclusions`) for privacy-sensitive users. | Docs updated with a per-flow "what reaches the LLM" table; if the env knob lands, a test asserts the request file does not contain `raw_text` content when the flag is off. | User may not realize their full profile description (including any personal context they wrote) is shared with ChatGPT during discovery. |
| 83 | P3 | [app.py:130](jobhunter/app.py:130) defaults `min_show_score` to 0 if `config/scoring.json` is missing or has no `thresholds.min_show_score`. After #76 fix this means a corrupt/missing scoring.json silently turns into "show every job" — every score ≥ 0. The DEFAULT_RULES in [scoring.py:13](jobhunter/scoring.py:13) has `min_show_score: 50`, so loading the empty default is safe; but a user-written scoring.json that omits the threshold would silently drop the floor. | Either: (a) raise the empty-thresholds default in [app.py:130](jobhunter/app.py:130) to 30 (sensible floor), or (b) fail loud at startup if `scoring.json` exists but is missing required keys (`rules`, `thresholds`) — log warning to stdout and Telegram-send a one-time "Scoring config malformed; using baseline." | With a `scoring.json` missing `thresholds`, the digest still applies a 30-point floor; with a `scoring.json` missing `rules`, the bot logs/sends a clear startup warning. | A misconfiguration silently loosens the filter; user sees lots of low-quality jobs and doesn't know why. |
| 90 | P2 | [watcher.js:10, 170-212](openclaw/worker/watcher.js:10) `inFlight` Set prevents the same session running twice but does NOT cap total concurrent Codex invocations. jobhunter's daily rate limit (3 discovery + 3 tuning per day) is a soft cap; rapid clicks within seconds could spawn 6 concurrent `codex` processes. Each uses ~512MB; with `mem_limit: 768m` the second concurrent run risks OOM. | Add a semaphore: `MAX_CONCURRENT = 1` (configurable via `OPENCLAW_MAX_CONCURRENT_CODEX`). Maintain a queue; `processRequest` waits if the semaphore is held. Mark queued sessions as `state=queued` in the status file so jobhunter can surface "queued, position N" if multiple are waiting. | Test: stubbing `runCodex` to take 30s; firing 3 requests in rapid succession produces 3 sequential codex spawns, not 3 concurrent. The third request's status shows `state=queued` then transitions to `running` then `done`. | OOM risk under rapid use; predictable serialization of LLM work. |
| 91 | P2 | [watcher.js:65-79, 165-167](openclaw/worker/watcher.js:65) `buildPrompt` produces the full prompt and pipes it to Codex stdin with no length cap. A very large request (long profile + many sources) can exceed Codex's context window; today this manifests as a Codex error returned through stderr, then `state=failed` — but the failure mode is opaque. | Cap prompt length at e.g. 60,000 characters (configurable via `OPENCLAW_MAX_PROMPT_CHARS`). If exceeded, skip the Codex call and write `state=failed` with `message="Prompt too large: <N> > <cap>"`. Update jobhunter's coordinators to also enforce a request-size cap before writing the request file (so the user gets the error sooner). | Test: a request with `profile_summary.description` padded to 100K chars produces `state=failed` with the size-cap reason and no Codex spawn. | Predictable failure mode for large profiles; saves Codex API call cost on guaranteed-fail invocations. |
| 92 | P2 | [openclaw/worker/Dockerfile:1, 11](openclaw/worker/Dockerfile:1) uses `node:22-slim` (moving tag) and `npm install -g @openai/codex` (always-latest). A future supply-chain incident in either updates your container on the next rebuild. | Pin both: `FROM node:22.11.0-slim@sha256:<digest>` and `npm install -g @openai/codex@<exact-version>`. Document a quarterly version-bump cadence in [AGENTS.md](AGENTS.md). | `docker compose --profile openclaw build openclaw-gateway` produces an image whose layer digests do not change unless the Dockerfile is edited. | Reproducibility + supply-chain hygiene. Critical given Codex CLI has direct subscription auth. |
| 93 | P2 | The shared workspace volume accumulates `request-*.json`, `codex-output-*.txt`, `status-*.json`, `response-*.json`, `handoff-*.md`, and `notified-*` files indefinitely. After hundreds of discovery/tuning cycles the host disk grows; the read-only-rootfs container can fail when its bound volume is full. | Add an archive step at jobhunter's startup or in a daily-trim path: move files older than 30 days under `<workspace>/discovery/archive/<YYYY-MM>/` (and same for tuning). Or simply delete files for sessions whose `discovery_runs.status` is one of `approved`, `rejected`, `failed`. Keep the most recent 50 of each kind unconditionally for debugging. | After running the trim, `find openclaw/workspace/discovery -maxdepth 1 -type f -mtime +30 \| wc -l` returns 0; archived files exist under the dated subdirectory. | Disk-fill incident risk in long-running deployments. |
| 94 | P2 | Today every discovery cycle is one-shot — Codex emits candidates, jobhunter HEAD-probes them at approve time, bad ones are silently `skipped_invalid`. Codex never learns from those rejections. The next discovery proposes the same kinds of URLs, hits the same kinds of failures. | Add a `previous_rejections: [{url, reason}]` field to the discovery request payload, populated from `discovery_runs` rows where `status='approved'` and the worker/jobhunter recorded a `skipped_invalid` reason. Cap to last 20 entries. Update [openclaw/prompts/discovery.md](openclaw/prompts/discovery.md): "These URLs were rejected at approval time. Do not propose URLs that would fail in the same way: [list]." | Test: after one approval cycle that rejects 2 candidates, the next discovery request file's `previous_rejections` contains those 2 URLs and reasons. Manual eval: Codex's next response avoids the rejection patterns. | Converts each approval round into training signal for the next cycle; reduces `skipped_invalid` count over time. |
| 95 | P3 | When jobhunter's HEAD probe fails or `looks_like_spa_shell` triggers at approve time, the candidate is silently dropped. Codex doesn't get a chance to find an alternative URL (e.g. the underlying API of an SPA, or the company's `boards.greenhouse.io` URL behind a custom domain). The user just sees fewer candidates than were proposed. The agentic loop the spec implies (validate → re-prompt with failure → propose alternative) is missing. | Two-pass validation in the worker. After Codex's first response: (a) for each candidate, worker does a HEAD probe + small GET; flag failures (HTTP error, SPA-shell heuristic, unparseable JSON). (b) Re-prompt Codex with the failed candidates and reasons: "These URLs failed validation. For SPAs, inspect the page source for `__NEXT_DATA__` / `<script type='application/json'>` / `/api/`/ `/_next/data/` paths and propose the underlying JSON endpoint instead. For custom-domain ATS pages, find the `boards.greenhouse.io` / `jobs.lever.co` / `jobs.ashbyhq.com` URL with the same content. Return replacements or mark candidates as `unparseable: true` with an explanation." (c) Worker writes the final response with successful candidates plus an `advisories` array. | Discovery test against a known SPA URL (e.g. `workatastartup.com/jobs`): the response either contains the underlying API URL OR an advisory explaining what was tried. Cap retries at 2 to bound cost. | The "Codex as URL detective" loop the spec implied. Likely doubles useful candidate count for non-trivial source ecosystems. |
| 103 | Partial | **Two integration scenarios that the agent surface MUST handle cleanly.** Use as the acceptance test for #97-#102 + #104-#105 working together. (a) `/agent please add to priority sources and figure out how to get content from there https://jobs.dou.ua/vacancies/?category=Product%20Manager` — needs: HTTP fetch of the URL, inspect for RSS/JSON endpoints (dou.ua exposes `/feeds/?category=...`), propose `sources_proposal` with `type=rss` + the discovered RSS URL, propose `directive_edit` adding `[<date>] Source priority: high — jobs.dou.ua` under `# Directives`. If the page is an SPA with no findable API, propose `human_followup` instead with a clear "needs custom collector" task description. (b) `/agent you missed https://weworkremotely.com/remote-jobs/webpt-principal-product-manager from 2 days ago. why? please adjust scraping approach not to miss such` — needs: SQL query for that URL in `jobs` and `digest_log`, SQL query of `source_runs` around that date, HTTP fetch of WWR RSS now to confirm whether the URL is/isn't there, root-cause analysis (RSS truncation? polling timing? parser bug?), produce `data_answer` with the root cause + propose either `sources_proposal` (e.g. switch from `type=rss` to `type=community` to scrape the full job board which doesn't truncate) AND/OR `human_followup` filing a "safety-net daily background poll" task. | Integration tests in `tests/test_agent_scenarios.py` using a deterministic Codex stub: scenario (a) produces the exact `proposed_actions` set (1× sources_proposal + 1× directive_edit) with the dou.ua RSS URL; scenario (b) produces `data_answer` with a root cause that names "RSS truncation" or "polling timing" + the right action set. Both scenarios complete within `OPENCLAW_AGENT_MAX_WALL_SECONDS=180`. | Locks the architectural contract. If these two requests work end-to-end, ~80% of realistic free-form requests work the same way. Forces every layer (worker tool surface, action handlers, approval UX, guardrails) to integrate correctly. |
| 105 | Partial | **Multi-turn discovery loop with cross-session knowledge base.** Was task #95. Today the discovery worker is one-shot: Codex returns 5 candidates, jobhunter HEAD-probes at approve time, failures get silently dropped. Codex never learns. | Convert the existing `discovery` worker handler to a multi-turn loop using the new tool surface (#104). Worker: read request → call Codex (turn 1, propose 5 candidates) → for each candidate call `http_fetch` to validate (HTTP code, SPA detection, parser-shape sniff) → if any fail, call Codex (turn 2, "these failed: <reasons>; propose replacements; for SPAs try `__NEXT_DATA__` / `/api/` / `/_next/data/` paths") → repeat once more if needed → write final response with successful candidates + an `advisories` array describing what was tried for failed candidates. Maintain `openclaw/knowledge_base/sources_tried.md` (append after every approve/reject decision, used as context in next discovery brief) and `openclaw/knowledge_base/scoring_history.md` (last 10 scoring versions + their drift outcomes, used in next tuning brief). Files mounted into worker container at `/openclaw/knowledge_base/` (rw); also gitignored. | Test: discovery against a known-SPA URL (`workatastartup.com/jobs`) — final response either contains the underlying API URL OR an advisory explaining what was tried. Knowledge base test: after one approval cycle, `sources_tried.md` contains the new entry with outcome. Next discovery sees it in the brief. Cap retries at 2 to bound cost. | Closes the "Codex returns valid URLs jobhunter can't parse" failure mode and the "Codex re-proposes the same failed sources" loop. |
| 112 | Partial | **Dual-run scoring window after Apply + drift alert + scoring history/revert + widen shadow-test.** Bundle of safety nets around scoring updates. | (a) For 7 days post-Apply (configurable `JOBHUNTER_SCORING_DRIFT_WINDOW_DAYS`), score every new job under both v_old and v_new. Persist diffs to `scoring_drift(job_id, v_old_id, v_old_score, v_old_hard_reject, v_new_id, v_new_score, v_new_hard_reject, scored_at)`. (b) Daily check: if ≥3 jobs in last 24h are hard-rejected by v_new but would have passed v_old with score ≥ threshold, send Telegram alert "Scoring v_N has hard-rejected M jobs that v_(N-1) would have shown. [Show diff] [Revert] [Keep]." (c) `/scoring history`: list versions with timestamps + headline shadow-test number. (d) `/scoring revert`: rolls to most recent archive; bumps version forward (v3 → v5) and inserts an audit row in `agent_actions`. (e) Widen shadow-test sample from 100 → 500 in `ScoringCoordinator.shadow_test`. | Tests: drift table accumulates rows; alert fires on ≥3 drifted jobs; revert restores the previous file and audit row appears in `/history`. | Catches "Codex's new rules silently filter out good jobs" — the user's explicit concern. |
| 114 | Partial | **`backup_export` action handler + post-Irrelevant "Why?" follow-up.** Two small UX/handler additions. | (a) `backup_export` payload: `{include: ['config'\|'input'\|'scoring_archives'\|'database']}`. Creates timestamped tar.gz at `data/backup/jobhunter-<ts>.tar.gz`. Returns path + size. Does NOT include `data/jobs.sqlite` by default unless explicitly requested (avoid huge files). (b) Post-Irrelevant follow-up: when user clicks Irrelevant on a job card, bot replies inline "Why? Reply within 60s with a one-liner, or skip." Next text message from the user within 60s gets prefixed with `[for job <id>: <title>]` and stored in `job_feedback.details`. This also flows into the next tuning request as `irrelevant_with_reason: [{job, reason}, ...]`. | Tests: `/agent backup my config` produces a tar.gz with the right contents. Post-Irrelevant: clicking the button sends the prompt; replying within window stores the reason; replying after window stores as a regular `/feedback` instead. | Completes the per-job feedback loop and gives users an easy backup escape hatch. |
| 120 | P2 | **Issue F from d1e9082 review: bulk-action CONFIRM flow is one-way refusal, not an actual confirm prompt.** [agent_actions.py:120-121, 173-174, 230-232](jobhunter/agent_actions.py:120) return `ActionResult(False, "...requires typed CONFIRM", requires_confirm=True)` when bulk thresholds are exceeded, but [app.py:435-491](jobhunter/app.py:435) `handle_agent_action` doesn't check `requires_confirm` or surface a CONFIRM prompt. The user sees "Updating more than 10 jobs requires typed CONFIRM" with no way to confirm. | Add a pending-confirm state: when an action returns `requires_confirm=True`, store `(user_chat_id, action_id, expires_at=now+60s)` in a small in-memory dict (or new `pending_confirms` table). Send Telegram message: "<action summary>. Reply `CONFIRM <action_id>` within 60s to proceed." Add a parser case in [telegram.py:243-272](jobhunter/telegram.py:243) that recognizes `CONFIRM <id>` as a `bot:confirm` action. The handler re-runs `apply_agent_action` with `payload['_confirmed'] = True`, and the action handler skips the bulk threshold check when `_confirmed` is set. | Tests: triggering a `bulk_update_jobs` matching 12 rows produces a CONFIRM prompt; replying `CONFIRM <id>` within 60s applies the bulk; replying after 60s does nothing. | Bulk operations are advertised but currently always refused. |
| 121 | P2 | **Issue H from d1e9082 review: `tasks.md` append from `human_followup` has no file lock.** [agent_actions.py:188-203](jobhunter/agent_actions.py:188) opens tasks.md in append mode and writes a new row with no `fcntl.flock`. Two `human_followup` actions in rapid succession (or while a human edits the file) could interleave writes. `next_task_id` ([line 324-331](jobhunter/agent_actions.py:324)) re-reads on each call, so two concurrent writes can both compute the same next id. | Wrap the read+write in a small file-lock helper using `fcntl.flock(LOCK_EX)` (Unix-only — acceptable for Linux containers). Compute `next_task_id` inside the lock. Archive (existing `archive_file`) before writing. | Test: two threads each call `human_followup` against a stub tasks.md → both writes land, ids are sequential, no interleaved garbage. | Single-user system today so probability is low, but agent-driven repeats or future multi-user setup would expose it. |
| 123 | P3 | **Issue from d1e9082 review: IPv6 SSRF gap in worker `http_fetch`.** [watcher.js:347-360](openclaw/worker/watcher.js:347) `assertSafeFetchUrl` checks IPv4 RFC1918 / loopback / link-local. IPv6 ULAs (`fc00::/7`) and link-local (`fe80::/10`) are not explicitly checked. Low risk because Docker default networking doesn't expose IPv6, but an SSRF target reachable via IPv6 (cloud metadata service) would slip through. | Add IPv6 family handling in `assertSafeFetchUrl`: if hostname looks like IPv6 (square brackets / colons), use Node's `net.isIPv6` and reject if `fc00::/7`, `fe80::/10`, `::1`, `::`. Or simpler: do DNS resolution first and check resolved address against both IPv4 and IPv6 reserved blocks. | Test: `http_fetch({url: "http://[fc00::1]/"})` is rejected; `http_fetch({url: "http://[::1]/"})` is rejected; legitimate IPv6 like `http://[2606:4700:4700::1111]/` is allowed. | Defense-in-depth gap; not exploitable on most Docker hosts today but cheap to close. |
| 124 | P2 | **Missing integration tests for #103 scenarios** (dou.ua "add and figure out access" + WWR-missed-job). Task #103 was filed as P0 to lock the agent surface contract; the architecture in d1e9082 supports both, but the regression tests don't exist yet. Future refactors of `runAgentCodex`, `KIND_HANDLERS`, or the worker tool surface won't catch breakage in these flows. | Add `tests/test_agent_scenarios.py` with two end-to-end tests using a deterministic Codex stub that returns canned tool_calls + final responses. Scenario A (dou.ua): stub returns `tool_calls: [{name: 'http_fetch', arguments: {url: 'https://jobs.dou.ua/...'}}]` then a final response with `proposed_actions: [{kind: 'sources_proposal', ...}, {kind: 'directive_edit', ...}]`. Assert the request file is written, the response is parsed, both actions appear in the approval prompt, and applying them updates `sources.json` + `profile.local.md`. Scenario B (WWR-missed-job): stub returns `tool_calls: [{name: 'query_sql', arguments: {sql: 'select * from jobs where url = ?'}}, {name: 'http_fetch', ...}]` then a final response with `data_answer` (root cause) + `human_followup` (filed task). Assert tasks.md gets a new row. | Both tests pass; running them under `JOBHUNTER_RUN_LIVE=0` doesn't hit Codex. | Locks the agent contract end-to-end. Without this, future refactoring breaks #103 silently. |
| 127 | P1 | **Source-specific parsing quirks keep being shipped as Python patches.** WWR's `Company: Title` colon separator was fixed by editing `infer_company` (#54). Garbage company-regex fallback was fixed by tightening Python regex (#61). Per-feed company selectors (#38, open) want bespoke selectors per source. SPA detection params (#81, open) want per-source heuristic tuning. Pattern: each new source quirk = code change + release + restart. The agent could observe a fetched page and propose a fix, but today it has no data shape to write into. | Move per-source extraction logic into bounded data, interpreted by fixed Python. (a) Add optional `extraction_config` field to source rows with a small DSL: `{title_strategy: {kind: 'split', separator: ': ', side: 'right'\|'left', stopwords: [...]}, company_strategy: {...}, description_extractor: {kind: 'regex'\|'css'\|'literal', pattern: ...}, spa_detection: {min_links: int, max_body_chars: int, framework_markers: [...]}}`. (b) Fixed Python interpreter in [sources.py](jobhunter/sources.py); default config preserves current behavior so existing tests pass. (c) New `source_extraction_proposal` action kind in [agent_actions.py](jobhunter/agent_actions.py): agent observes a fetched sample (via `http_fetch`) and proposes a config; user approves; persisted to the source row. (d) Schema validation rejects unknown DSL keys at apply-time. Supersedes #38, #81. | Source row with `extraction_config: {company_strategy: {kind: 'split', separator: ':', side: 'left'}}` extracts company=Toptal from "Toptal: QA Automation Engineer" without touching `infer_company`. Agent flow: `/agent the wwr scraper is grabbing 'Unknown company' for X` → produces `source_extraction_proposal` action → approval persists the new config. Existing tests pass. New tests for the DSL interpreter and the action kind. | Stops shipping releases for every new source quirk. Lets the agent self-improve scraping quality. Closes a recurring class of code-vs-config friction. |
| 128 | P0 | **Email parsing still produces email-as-job rows in the digest.** Verified live 2026-05-05: 5 of the top 10 in the digest are email subjects parsed as single jobs (e.g. "Serhii: your job alert for Ai Product Manager", "product engineer contract or…": WeSuite). Each is one DB row with l1+l2=92 because the email SUBJECT contains "Product Manager" / "AI" — so the rule matches fire on subject text instead of real job titles. **Most of the infrastructure already exists** (built in earlier commits): `email_templates` table, `email_parser_configs` table, `email_template_matches`, `jobs_from_template` interpreter, `email_parser_proposal` action kind in `KIND_HANDLERS`, `email_templates_for_source` query. **What's broken**: only 1 template row exists (`scoped-job-alert-mailbox-generic`) with regex like `^title:\\s*(.+?)$` that doesn't match real djinni / LinkedIn / indeed alert structure → falls back to generic URL harvester → email-as-job artifacts. **Why the agent isn't fixing it autonomously**: Codex would need to read a real sample email body to author a parser config, but raw email bodies aren't persisted anywhere it can `read_file` or `query_sql`. | Two small additions on top of existing infrastructure. (a) **Persist raw email bodies** — when IMAP fetch lands an email, also write it to `data/email_samples/<sender_slug>/<subject_slug>-<msgid>.html` (ring buffer, last 20 per sender, gitignored). Add the `data/email_samples` path to the worker `read_file` allowlist. (b) **Wire `email_parser_proposal` end-to-end through Telegram approval** — verify the existing handler in `agent_actions.py` writes new rows correctly, and that the approval keyboard renders payload preview meaningfully. (c) Update `openclaw/prompts/agent.md` with the workflow: "if user complains about email parsing or you see email-as-job artifacts, `list_dir('data/email_samples')` to find the sender, `read_file` a sample, propose `email_parser_proposal` with sender+subject patterns and a parser_config that extracts real job-card structure." (d) After first real per-sender template is approved, delete the placeholder `scoped-job-alert-mailbox-generic` template — it's not actually parsing anything useful. | Live test: open the bot, the 5 email-as-job rows from the latest digest no longer reappear after IMAP refresh because the agent has authored a per-sender template that yields N real job rows per email. `data/email_samples/` contains recent samples. `/agent please add a parser for the latest djinni email` produces a proposal, you approve, future djinni emails parsed correctly. New tests: (i) sample persistence on IMAP fetch, (ii) read_file allowlist for email_samples, (iii) end-to-end agent flow with deterministic Codex stub. | Eliminates the email-as-job noise that's polluting the top of every digest. Closes the self-improvement loop for new email templates without code releases. The "Serhii: your job alert..." rows disappear because real job-card extraction replaces email-subject extraction. |
| 133 | Done | **`config/sources.json` and `config/scoring.json` are tracked-as-baseline but mutated-at-runtime.** Every collection cycle / tuning apply / discovery approval mutates these files; `git status` is dirty after any real use, you can't distinguish baseline edits from runtime drift, and committing accidentally bakes in test outcomes. The same anti-pattern is already solved for profile via `input/profile.example.md` (committed) + `input/profile.local.md` (gitignored). | Mirror the profile split for both files. (a) Rename committed baselines to `config/sources.example.json` and `config/scoring.example.json`. (b) Add `config/sources.local.json` and `config/scoring.local.json` to `.gitignore`. (c) `JobHunter.initialize` copies `*.example.json` to `*.local.json` if missing. (d) Defaults in `JOBHUNTER_SOURCES_PATH` and `JOBHUNTER_SCORING_PATH` point to the local file; example file is never written by the runtime. (e) Tuning-archive convention stays (`scoring.v<n>.json` next to the local file). (f) Update [config.py](jobhunter/config.py) loader, Dockerfile COPY (still copies examples), and any tests that point at the old names. | After init, `config/sources.local.json` and `config/scoring.local.json` exist and are in `.gitignore`. After a discovery approval or tuning apply, `git status` shows no dirty config/ files. The 5 baseline sources still load by default for new users (from the example). | Stops the recurring "git status is dirty after running the bot" problem. Cleans up the commit history. Makes the baseline immutable from runtime. |
| 137 | P1 | **L1 anomaly detector — issues sit in logs unnoticed.** Late-ack `Bad Request` (sessions 20260503044533362346 at 04:48 + 05:09), `Robots.txt disallows` failures, the `TimeoutError` crash loop — all of these sat in container logs for hours/days before being noticed during manual retest. There's no automated detection: the user only finds out when they manually trigger a flow that fails. Foundation for #138 (deep analysis). | Add a periodic anomaly detector to the existing telegram poll loop (every 5 min). Pure SQL + log-tail in Python, no LLM. Signals: (a) `source_runs` with `status='failed'` in last hour > N, (b) `agent_runs` with `status='failed'` since last scan, (c) Telegram retry spikes from stdout grep, (d) heartbeat stale > 90s, (e) empty-digest streak (last K consecutive `Get more jobs` returned 0 jobs), (f) workspace file count growth > threshold, (g) crash signatures (`Traceback`, `ERROR`) above baseline. Each detection produces structured anomaly: `{kind, count, first_seen, last_seen, evidence_excerpt, fingerprint}`. Persisted to new `anomalies(id, kind, fingerprint, count, first_seen, last_seen, status, l2_session_id, evidence_json)` table; deduped by fingerprint within 24h. Disable per-kind via `JOBHUNTER_ANOMALY_DISABLE=kind1,kind2` env. | Restart bot with a deliberately broken source (bad URL), wait 5 min, verify a row appears in `anomalies` table with kind=`source_failure`. New tests for each detector kind. No false positives on a healthy run. | Closes the "issue sat in logs unnoticed" gap. Foundation for #138. |
| 138 | P1 | **L2 deep analysis + `taskcandidates.md`.** An anomaly tells you something's wrong but not what to do about it. After #137 detects an anomaly, need investigation + concrete task suggestion. | When #137 detects a NEW anomaly (deduped against open `taskcandidates.md` entries by fingerprint), jobhunter writes `log-review-<sid>.json` to `openclaw/workspace/log-review/` with the anomaly + relevant log excerpt + tool-list hint. Worker handles `kind: 'log_review'` (mirror existing agent kind, same tool surface — no new infra). Codex investigates: `read_file` related Python source, `query_sql` related tables, `http_fetch` if anomaly references URL. Returns 1-3 candidate task rows in the same `\| # \| Priority \| Problem \| Solution \| Acceptance \| Impact \|` format. jobhunter appends them to `taskcandidates.md` with header `<!-- scan: <sid> at <ts> kind=<anomaly_kind> -->`. New Telegram bot verbs: `/scan show` lists open candidates with id + summary, `/scan promote <n>` moves candidate N into `tasks.md` (assigns next available number), `/scan dismiss <n>` archives it. Cap candidates at 5/scan; total open candidates at 50 (auto-archive oldest). | Trigger anomaly from #137, verify Codex investigation completes within 60s and a row appears in `taskcandidates.md`. `/scan show` lists it. `/scan promote 1` moves it to `tasks.md` with the next available number. New tests: (i) `log_review` request roundtrip, (ii) `/scan` verbs, (iii) candidate dedupe by fingerprint, (iv) auto-archive at cap. | Closes the "diagnose → file" loop. User wakes up to a list of pre-investigated candidates, not a pile of raw errors. |
| 139 | P2 | **`code_change_proposal` action + PR opener via `gh` CLI.** After #138 lands, candidates accumulate as text. User still has to do the implementation work (or hand to a coding agent). For trivial fixes (typos, doc updates, one-line bug fixes), the diff often already exists in Codex's investigation — opening a PR is the next natural step. Codex stays read-only; jobhunter mediates the PR creation, same trust model as the existing approval-button flow but delivered through GitHub. | New action kind `code_change_proposal` with payload `{file_path, unified_diff, summary, test_plan}`. Codex emits via existing `proposed_actions` mechanism. jobhunter validators in [agent_actions.py](jobhunter/agent_actions.py): (a) `file_path` matches allowlist (`jobhunter/**`, `tests/**`, `openclaw/prompts/**`, `config/*.example.json`, `*.md`), (b) `file_path` NOT in deny-list (`.env`, `bin/jobhunter`, `Dockerfile`, `docker-compose.yml`, `.github/workflows/**`), (c) patch < 200 lines, (d) no secret patterns in diff (regex for AWS/GH/OpenAI key shapes), (e) applies cleanly against `main` HEAD. If validation passes: jobhunter creates a temporary worktree, applies the patch, runs `python3 -m unittest discover -s tests`. Only opens PR if green. PR opened via `gh pr create` (requires `GITHUB_TOKEN` in `.env`, fine-grained PAT scoped to this repo with `contents:write` + `pull-requests:write`, no admin). Branch protection on `main` (require PR review before merge) is the human gate. If validation fails: file as `taskcandidates.md` entry instead with the failed-validation reason. | Codex returns `code_change_proposal` for a typo fix in README → jobhunter validates, applies, runs tests (pass), opens PR. PR appears on GitHub; tests run in CI; no auto-merge. `code_change_proposal` for `bin/jobhunter` is rejected with "deny-list" reason. Test verifies validators with mocked `gh`, not real PR opening. README documents the GitHub PAT setup + branch protection requirement. | Codex can deliver actual fixes for trivial issues without manual implementation. Same trust model as Telegram approval — user reviews PR before merge. |
| 140 | P3 | **Auto-merge for trivial PRs.** After #139 lands, every PR (even a typo fix) requires a click. For provably-safe changes (test-only additions, comment-only changes, doc-only changes), the human-review tax can be skipped. Defer until #139 has a proven track record (3+ months of clean operation). | After a PR opened by #139 passes CI, jobhunter checks if the diff is in a "trivial-safe" class: only `*.md` changes, only comment lines added/removed in `.py` (verified by `grep -v '^[+-][[:space:]]*#'` returning zero diff lines), only test file additions (no source modifications). If yes: auto-merge via `gh pr merge --merge --delete-branch`. Anything else: leave for human review. Override knob: `JOBHUNTER_AUTO_MERGE=disabled` to turn off entirely. | PR with only README typo fix + green CI → auto-merged. PR with `.py` source changes → not auto-merged. Test verifies the trivial-safe classifier. | Removes friction for trivial changes. Defer until #139 has a track record. |
| 141 | Partial | **Silent rate-limit refusal.** Tap Get more jobs while in the 600s cooldown → telegram_action_received logs but no follow-up. Verified at 09:19:20: handler called `answer_action(action, "Please wait Ns")` ([app.py:236-238](jobhunter/app.py:236)) which routes to `send_message` for text actions, but message never reached the chat. Same for any handler that uses `answer_action` after a refusal — failures aren't logged either. | Implemented core fix: `answer_action` logs send failures, `handle_action` has a top-level exception fallback, unknown action shapes log `action_outcome`, and bot-level rate-limit/queue/serve paths log outcomes. Remaining gap: not every `handle_agent_action` and `handle_job_action` terminal branch has a specific outcome line yet. | Tests cover Get more jobs cooldown producing a Telegram wait message and `action_outcome outcome=rate_limited`; full branch audit remains follow-up. | Fixes the "stuck" UX from session 09:19:20 for the known rate-limit path; diagnostic coverage is broader but not exhaustive. |
| 142 | Partial | **Index-then-serve architecture.** Today: collect runs L1, then digest time runs L2 lazily on top-30 candidates → Get more jobs takes 30-90s and produces inconsistent results because L2 is half-applied. Need to flip: index eagerly (collect + L1 + L2 all at index time), serve from cache. | Implemented safe slice: collection indexes capped L2 for new jobs above L1 cutoff, digest is pure SELECT, Get more jobs instantly serves indexed jobs and queues background refresh when stale, `/refresh` forces refresh, and `JOBHUNTER_COLLECT_STALE_MINUTES` controls the stale gate. Remaining gap: scoring/profile rescore currently runs synchronously for preview rather than fully background. | Tests cover indexed digest, stale refresh routing, and updated command parsing; a deeper async-rescore test remains. | Big UX improvement landed without a risky orchestration rewrite. |
| 143 | Partial | **Behavior + smoke + perf tests.** Current tests are wiring tests (does function A call B with right args). They missed: scoring v3 hard-rejecting 204 jobs (#scoring_drift), silent rate-limit refusal (#141), Codex never using tools (#130 partial), all baseline sources blocked by robots.txt (#131 follow-up), agent-added sources blocked by robots.txt (#144). Each was a real user-visible regression that shipped. | Added `tests/test_behavior.py` for top-10 fallback, L2 demotion visibility, snoozed resurfacing, and 1000-job digest query performance. Added `tests/test_smoke.py` gated by `JOBHUNTER_RUN_LIVE=1` for live source parseability. Remaining gap: no CI workflow wiring and no live deterministic agent smoke. | Local unittest suite passes with smoke skipped unless opted in. | Better regression net for the exact failures seen, with CI/live-agent hardening still open. |
| 144 | Done | **Agent-added sources blocked by robots.txt.** Last interaction: agent added 3 himalayas.app sources via `sources_proposal`; next collection failed all of them with `Robots.txt disallows...`. Root cause same as #131 for baseline sources: `risk_level` defaults to None (or unset), and the robots policy `not (created_by=='user' or risk_level=='low')` evaluates to True (robots-checked) for agent-added rows. Net effect: every Codex-discovered source dies on first collection. | `sources_proposal` and legacy discovery now default agent-added sources to `risk_level='low'`; prompt tells Codex to set risk explicitly; collection warns Telegram when an agent-added source fails its collection test. | New test verifies a source proposal without risk_level lands as low risk. | Restores agent's "discover and add a source" capability for vetted public feeds. |
| 145 | Partial | **No preview after scoring/profile change.** When scoring_rule_proposal applies, jobhunter rescores existing jobs ("Rescored 353 jobs; up=19, down=204") but the user has to manually tap Get more jobs to see the impact — and yesterday they got 1 job after a v3 apply that pushed 204 jobs below threshold. By the time the user notices, the digest is empty. | Implemented top-5 preview after scoring/profile applies and confirmed applies; includes both `/revert <id>` guidance and an inline Revert button when an agent action id is available. Remaining gap: legacy tuning apply has no agent action id, and profile L2 does not forcibly invalidate cached L2 verdicts yet. | Preview is sent immediately after apply; agent-action previews include a tap-to-revert button. | Makes scoring/profile changes visible immediately, with legacy tuning revert still command/audit limited. |
| 146 | Partial | **Unified scoring pipeline (the big refactor).** Today's pipeline mixes hard filters (`min_show_score`, `hard_reject`, L2 verdict allowlist, digest_log filter), categorical overlays (L2 verdict tiers, L2 priority tiers), and compound sort (`priority desc, score desc, first_seen desc`). Failure modes are silent: scoring v3 misset → empty digest → no idea why. The user proposed a cleaner model: one score 0-100 (L1: 0-50 + L2: 0-50), no hard filters, sort by score, always show top 10. Bugs become visible because nothing is silently filtered. | Implemented v7/v8 columns on `jobs` (`l1_score`, `l2_score`, generated `total_score`, `l2_reason`, `l1_rules_fired_json`), backfill, queryable unified rank, digest order by generated total score, no `min_show_score`/hard_reject/L2 verdict filter in digest, and score card shows L1+L2. `min_show_score` now logs a deprecation warning when loaded. Remaining gaps: L2 still returns verdict/priority internally and maps to numeric score, `hard_reject` remains transitional audit data, and `config/scoring.json` may still contain thresholds for compatibility. | Tests cover generated total-score integrity, top-10 fallback, total-score ordering, L2 demotion, snoozed resurfacing, deprecation warning, and digest query performance. | Removes the silent-empty digest mode while keeping the refactor low-risk and backward-compatible. |
| 147 | Done | **Cumulative prompt growth across agent turns blows the 60K cap.** Codex's [14424468b58](https://github.com/ihvou/jobhunter/commit/14424468b58) added per-turn tool-result compaction (file content cap, SQL row cap, 3-tier summarization). But the prompt is APPENDED to across turns — `workingPrompt += block` ([watcher.js:357](openclaw/worker/watcher.js:357)). After 3 turns of compacted-but-substantial results, total exceeds maxPromptChars=60000. Verified live: 13:21:36 and 13:26:38 both failed with `Prompt too large: 68125/68015 > 60000` AFTER Codex's compaction shipped. User sees the friendly error from #135 fix but can't proceed. | `runAgentCodex` now keeps tool-result history separately, rebuilds the prompt each turn, keeps recent full turns, and replaces older turns with `summaryToolResult` summaries when the prompt grows. | New test verifies old tool-result turns are evicted while the latest turn remains full and the prompt stays below cap. | Closes the actual user-visible multi-turn prompt-too-large failure without reverting 14424468b58. |
| 148 | Done | **Robots.txt restriction is the wrong tool for this bot.** Verified live: agent added 4 himalayas.app sources; 3 of 4 had `risk_level: medium` (added before #144 fix shipped) and were blocked with `Robots.txt disallows...` on every collection. Backfilled to `low` manually via SQL today. But the bigger issue: robots.txt was designed for search-engine crawlers that follow links recursively across millions of pages. This bot makes 5-10 requests per source per day total, fetches ONE URL per source (no crawl), and operates on documented public APIs / RSS / ATS feeds that are explicitly designed to be queried. Other safety measures already cover the "don't be abusive" axis: per-host rate limiter (1 req/2s), 8MB response cap, 30s timeout, SSRF protection. Robots.txt produces only false positives for this use case, and the workaround is repeatedly setting `risk_level: low` after the fact. | Default `JOBHUNTER_ROBOTS_TXT_RESPECT` is now `ignore`; `trust` and `strict` remain opt-in. Schema v9 backfills `created_by='agent'` sources with empty/medium risk to `low`. README/ARCHITECTURE document the non-crawler model and opt-in robots policy. OpenClaw prompts no longer tell Codex to validate via robots.txt or force `risk_level: low` for public feeds. | Tests cover default ignore config, ignored robots check under default policy, strict robots failure, and v9 agent-source risk backfill. | Stops the recurring "agent added a source, can't reach it, why?" surprise. Removes a class of source-failure that's been confusing for weeks. The "be polite" intent is satisfied by rate limiter + caps without robots.txt theater. |
| 149 | Done | **Post-Irrelevant "Why?" follow-up overwhelms when marking multiple jobs.** [app.py:466](jobhunter/app.py:466) sends "Why was it irrelevant? Reply with a one-line pattern..." after every Irrelevant click. When user marks 5 jobs irrelevant in a row, they get 5 follow-up prompts, each requiring a reply or being ignored. Confusing UX. Came from #114 (Partial). | Gate the follow-up message on a new env knob `JOBHUNTER_IRRELEVANT_FOLLOWUP=0\|1` (and `irrelevant_followup_enabled` setting). Default OFF until we have a smarter trigger (e.g., only ask for the highest-score-Irrelevant of a session, or only after a 30s pause, or only for jobs with score > 70). | Marking multiple Irrelevant jobs in a row produces no follow-up prompts when knob is off (default). Setting `JOBHUNTER_IRRELEVANT_FOLLOWUP=1` re-enables the prompt. | Removes the immediate annoyance without losing the underlying "capture irrelevance reasons" capability — re-enable later when smarter trigger ships. |
